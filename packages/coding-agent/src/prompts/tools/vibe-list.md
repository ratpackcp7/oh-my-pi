Worker-session roster: id, role/cli (`scout`/`utility`/`implementer`/`designer`/`planner`/`reviewer`/`fast`/`good`), state (`starting`/`running`/`idle`/`dead`), model (planned vs actual), intent, external task link, turn/queue counts, gist of latest activity.

Use to reorient: existing sessions, busy workers, idle workers ready for next instruction.
