# Design-bench smoke task (deliberately tiny fixture)

This is an end-to-end plumbing check for the design-bench wrapper, not a
design task. Do exactly this and nothing more:

1. Create a directory named `proofs`.
2. Create two text files inside it, each containing the single word `done`:
   - `proofs/proof-a.txt`
   - `proofs/proof-b.txt`
3. Reply with one sentence confirming the files were written.

Do not read or modify any other files.
